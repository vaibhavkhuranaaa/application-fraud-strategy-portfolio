# Metric glossary

Status: `M3 model, strategy, drift, fairness, and linking results verified; M4 interface quality and M5 release quality measured; comparator, calibration-schedule, and stability-reference corrections applied at M6 on 2026-08-10; segment measurement completed at M7 on 2026-08-10 with the governance decision still open; no HTTP API exists, so its latency and concurrency targets have nothing to measure`

| Metric | Definition / method | Gate | Verified result | Supported decision / limitation |
| --- | --- | --- | --- | --- |
| PR-AUC | Area under the precision-recall curve at natural month-7 prevalence | Paired 95% lift CI lower bound >0 | Hybrid 0.2129; regularized logistic 0.1787; incumbent 0.0403; prevalence 0.01475; lift over the strongest baseline 0.0342 with CI 0.0219–0.0464 from 1,000 stratified resamples | Challenger ranks rare fraud better; prevalence-dependent and not a policy threshold. The lift is measured against the linear comparator, which is the strongest baseline since its M6 correction |
| AUROC | Probability a fraud ranks above a good application | Secondary, higher is better | Hybrid 0.8947; regularized logistic 0.8848; incumbent 0.6762 | Discrimination diagnostic; can overstate usefulness on imbalanced data |
| Brier skill | `1 - model Brier / prevalence Brier` | >0 | Hybrid Brier 0.012878; skill 0.1136 | Probability accuracy passed versus prevalence |
| ECE | Weighted absolute predicted/observed gap across 15 fixed bins | <=0.02 | Hybrid 0.00226; incumbent 0.00197 | Passed; bin-dependent |
| Calibration slope/intercept | Logistic recalibration of outcome on model logit, read at probability 0.5 | Slope 0.8–1.2; absolute intercept <=0.10 | Hybrid slope 1.041, intercept 0.301; incumbent slope 1.037, intercept 0.299; regularized logistic slope 1.037, intercept 0.480 | Slope passed, intercept failed; blocks promotion. Every comparator misses the intercept gate, so month 7 shifted for all of them. The hybrid's 0.301 is 0.2325 slope term plus 0.0689 of level error, because the reading position sits 5.66 logits above where these scores are. The incumbent is retained as the simpler baseline, not because it calibrates better |
| Calibration schedule | Prior shift between the calibration period and the scoring period, forecast rule chosen by backtest on months 3 to 6 | Operating control, not a gate | Carry-forward selected at 0.1079 mean absolute logit error against 0.1065 for a damped trend, inside the 0.02 margin; applied shift 0.0000 | Recalibrate every period close; raise a review when the observed prior moves past 0.10 in logit. Median observed move 0.0992, exceeded in 3 of 6 transitions |
| Fraud catch rate | Fraud routed to review/referral divided by all fraud | Beat incumbent at 3/4 capacities and no >2 pp regression | Hybrid 21.9% / 41.7% / 53.6% / 67.8% at 1% / 3% / 5% / 10%; regularized logistic 18.7% / 37.4% / 49.4% / 64.8%; incumbent 6.0% / 14.9% / 20.6% / 30.8% | Passed all four comparisons; retrospective synthetic labels. The linear comparator reaches 92.0% of the hybrid's catch at 5% capacity |
| Precision at capacity | Fraud caught divided by actioned applications | Higher at bounded capacity | Hybrid 32.3% / 20.5% / 15.8% / 10.0% at the four capacities | Helps estimate investigator yield; not approval precision |
| False-positive rate | Good applications actioned divided by all good applications | Lower at equal capacity/catch | Hybrid 4.27% at 5% capacity | Review is a friction proxy, not observed abandonment |
| Queue overflow | Forced cases beyond manual-review capacity | Zero for a recommendation | Score-only policies: zero; rule-enabled policy at 5%: 39,282 referrals beyond capacity | Broad concentration rules are infeasible and cannot decline automatically |
| Incremental utility | Challenger utility minus incumbent under the approved exposure/review/friction grid | Positive for >=80% | Positive at 60/60 points at 5%; $2.36M–$9.58M | Ranking economics passed, but values are assumptions - not observed P&L |
| Segment review | TPR/FPR gaps and review-rate ratios for groups with >=200 positives | Governance review above 10 pp or outside 0.80–1.25 | Housing: review-rate ratio 0.073, TPR gap 0.377, FPR gap 0.156. Age: ratio 0.397, TPR gap 0.234, FPR gap 0.049 | Unresolved; blocks recommendation, not a legal fairness finding. Measured in full at M7 and still routed to a human |
| Prevalence-adjusted review-rate ratio | Review-rate ratio divided by the same groups' fraud-rate ratio, isolating disparity not explained by where fraud is | Read against the same 0.80–1.25 band, diagnostic rather than a gate | Age 0.836, inside the band; housing 0.484, outside it. Housing reaches 1.119 with `housing_status` removed | Separates a prevalence artifact from a model behaviour. Not a legal standard and not a substitute for the contract gate, which reads the raw ratio |
| Feature-ablation cost | Paired refit with one feature removed, identical months, rows, seeds, and tuned parameters; decided on rolling folds with month 7 recorded but not read | Materiality fixed before the run at 1.0 pp catch at 5% capacity and 0.005 mean rolling PR-AUC | Removing `housing_status` costs 2.39 pp of catch at 5% capacity and 0.0120 mean rolling PR-AUC; 4.41 pp and 63 of 1,428 fraud cases on month 7 | Material under both thresholds. Measures one feature on one dataset; the removal also cuts total detection rather than only redistributing it |
| Group proxy recoverability | AUROC of a model predicting group membership from the features that remain after the group attribute is dropped | Diagnostic; no gate | Housing BA 0.799, BC 0.770 | Shows that dropping a group attribute does not make the model blind to the group, which bounds what removal can achieve |
| PSI | Distribution shift from the pooled training window, months 0 to 5, using fixed bins | Warn >=0.10; block >=0.25 | 12 warnings and 16 blocks across feature/score-month checks, 9 of them after the training window | Blocks automatic promotion pending investigation. The month-0 reference this replaced reported 43 and 32, half of which measured the training window against its own earliest month |
| Pairwise linking F1 | Harmonic mean of pair precision/recall against withheld fixture truth | >=0.90 clean; >=0.80 at 15% corruption | 1.000 clean; minimum 0.953 at 15% | Passed; applies only to the deterministic fixture |
| False-merge rate | Accepted different-entity pairs divided by accepted pairs | <=2% | 0.0 through clean, 5%, and 15% regimes | Passed on fixture only |
| B-cubed F1 | Per-record cluster precision/recall harmonic mean | Report all regimes | 1.000 clean; >0.999 at 15% for the primary seed | Fixture cluster quality, not BAF ring recovery |
| Link runtime | Matching/evaluation time for one 50,000-row fixture | <5 minutes | Maximum 1.304 seconds across required seeds/regimes | Excludes fixture generation and PostgreSQL network latency |
| Curation duration | Checksum validation, normalization, IDs, Parquet write/read-back | Reproducible full-suite run | 19.787 seconds for 6,000,000 rows; 467 MB output | Local Apple silicon result; not the one-million-row scoring SLO |
| Single-record score latency | p95 <250 ms | M5 operational gate | Challenger p95 0.357 ms over 300 samples | In-process, not an API. Not measurable for the retained champion, whose mapping standardises against the batch it receives |
| Batch scoring | 1,000,000 rows <15 min | M5 operational gate | 0.53 s; month-7 slice matches the recorded prediction artifact exactly (max difference 0.0) | Local reference machine; single pass, no I/O contention |
| Scenario computation | p95 <2 s | M5 operational gate | p95 16.4 ms over the 96,843-row month-7 period | Warm in-process data; excludes browser rendering |
| API latency and concurrency | p95 scoring <250 ms under load | Deferred | Not measured | No HTTP API and no load-test harness exist, so there is nothing to measure |
| Dashboard first render | First meaningful render <3 s | M4 design gate | Median 42.3 ms at four viewport widths locally; on throttled Fast 3G with gzip, first paint 204 ms and the decision readable at 261 ms | Headless Chromium. Styles, script, and above-the-fold facts are inlined, so the decision arrives in one request |
| Interface contrast | 4.5:1 body, 3:1 large text | M4 design gate | Zero failures across 857 text nodes; worst 5.11:1 | Computed from rendered colours; does not replace human review of meaning. The surface commits to one light rendition |
| Interface responsiveness | No horizontal page scroll at 390/768/1280/1680 px | M4 design gate | 0 of 4 viewport widths scroll | Wide tables scroll inside their own container by design |

Evidence: `evaluation/model_evaluation.json`, `evaluation/linking_evaluation.json`, `evaluation/data_curation.json`, `evaluation/ux_evaluation.json`, and `evaluation/release_quality.json`. Every public rendering must carry population, period, source, assumption, confidence, and limitation context.

Incumbent calibration figures were corrected on 2026-08-09 after its score mapping was fixed to use
fixed calibration-period reference statistics instead of re-deriving them from whatever batch it was
handed. Its earlier ECE of 0.000096 was an artefact of scoring month 7 against month 7's own
distribution and must not be cited. Rank-based metrics were unaffected. See the correction section in
`evaluation/report.md`.
